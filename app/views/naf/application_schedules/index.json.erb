<%
  rows = @application_schedules.each do |schedule|
		app = Naf::ApplicationSchedule.find(schedule.first)
		schedule[0] = link_to schedule[0], application_schedule_path(schedule[0])
		schedule[1] = link_to schedule[1].title, application_path(schedule[1])
  end
%>
<%= raw rows %>
